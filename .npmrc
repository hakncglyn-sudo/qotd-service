; Use bash as the npm-script shell on Windows so single-quoted args
; (e.g., jq filters in claude:review) survive cmd.exe quoting differences.
; On macOS/Linux this is a no-op since bash is already the default.
script-shell=bash
